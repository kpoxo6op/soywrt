all:
  children:
    openwrt:
      hosts:
        home-router:
          ansible_host: 192.168.1.1
          ansible_user: root
          ansible_ssh_common_args: "-o PreferredAuthentications=publickey"
